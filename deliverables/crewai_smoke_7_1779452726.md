# PC Agent test: list running processes

I’m unable to execute the PC Agent command sequence or retrieve the requested process list because command submission to the relay is failing—the relay is returning “Missing command” errors despite multiple PowerShell payload attempts. This indicates the relay expects a different request format than the one currently available to me. Without successful relay communication, I cannot interact with the PC Agent to collect the Chrome process data.